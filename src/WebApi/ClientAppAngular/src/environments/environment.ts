export const environment = {
  production: false,
  // Base API url used by the Angular app. Set to '/api' by default so service
  // calls become '/api/players' etc. Can be overridden in production build.
  apiBaseUrl: 'https://localhost:5000/api'
};
